{
  "name": "node-red-contrib-freed-parser",
  "version": "1.0.0",
  "description": "A UDP packet parser for freed data.",
  "main": "parse-freed.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "node",
    "red",
    "freed",
    "free-d",
    "parser",
    "camera"
  ],
  "author": "Evan Clark",
  "license": "MIT",
  "node-red": {
    "nodes": {
      "freed-parse": "freed-parser.js"
    }
  }
}
